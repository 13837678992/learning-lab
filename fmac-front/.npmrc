# pnpm workspace 配置
auto-install-peers=true
strict-peer-dependencies=false
# esbuild 的安装脚本被 pnpm 默认拦截，但其二进制随平台包（@esbuild/<platform>）分发、无需构建即可用；
# 关闭「运行脚本前校验依赖」以免该拦截阻断 pnpm dev / build / lint。
verify-deps-before-run=false
